/**
 * graph-core placeholder test — Phase 0
 *
 * Ensures `vitest run` passes even before Phase 2 implements the real algorithms.
 * All substantive tests (cycle detection, Kahn's algorithm, fixtures) are added in Phase 2.
 */

import { describe, it, expect } from 'vitest';

describe('graph-core (Phase 0 placeholder)', () => {
  it('package scaffolded correctly', () => {
    // The real algorithms are added in Phase 2.
    // This test exists so `pnpm -r test` does not fail during Phase 0.
    expect(true).toBe(true);
  });
});
